/**
 * Cover stub — Phase H.5 deliverable.
 *
 * Concept (locked at Checkpoint 3, deferred for authoring):
 * The Wine-Panel Grid — 4×3 dot grid, 11 terracotta-filled (eval passes),
 * 1 empty (the failure that didn't show up). Doubles as a tasting ballot
 * and a CSV column. Contemplative register, ~14 elements, reads at 64px
 * because the empty cell is the only contrast.
 *
 * The animated default (`EvalsOrVibesCover`) and static sibling
 * (`EvalsOrVibesCoverStatic`) will be authored in
 * `components/covers/EvalsOrVibesCover.tsx` per the H.5 phase, and the
 * registry entry added to `components/covers/PostCover.tsx`.
 *
 * This file is intentionally empty so the directory is committed; the
 * post page does not import a cover (the post-PR-#92 convention is no
 * `<HeroTile>` on the article page; the cover renders only on the
 * homepage tile via `<PostCover slug={...} />`).
 */
export {};
